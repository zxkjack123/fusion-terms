# sources

This repo does **not** store your entire Markdown corpus by default.

Your corpus lives at (current default):

- see `config.toml` (`[sources].root`) and set it to your local pdf2md output directory

If you ever want to track a subset, put small curated snapshots here, or add a pointer file such as `sources/local_paths.txt` (gitignored).
