# IME Acceptance Record — fix-rime-mixed-term-encoding (2026-08-17)

- generated_date: 2026-08-18
- plan: .github/plans/fix-rime-mixed-term-encoding-2026-08-17.md
- related_commit (Phase 1-3): see plan Execution Log
- payload: artifacts/.rime_import_rime_ice.txt (2114 entries)

## Environment / 执行环境

| 项目 | 值 |
|------|----|
| rime 目录 | `~/.config/fcitx/rime/`（唯一存在；`~/.config/ibus/rime/` 不存在） |
| custom_phrase.txt | `~/.config/fcitx/rime/custom_phrase.txt` |
| 修复前混排计数 | **75**（总行数 2499；含第 345 行 `ITER到DEMO	dao	10000` 与第 62 行 `Go程	gocheng`） |
| 修复后混排计数 | **0**（总行数 2424） |
| 首 3 行头保留 | ✅ `# Rime table` / `# coding: utf-8` / `#@/db_name	custom_phrase.txt` |
| 备份文件 | `~/.config/fcitx/rime/custom_phrase.txt.bak-20260817`（权限 664，与原件一致） |
| 词库重建 | `rime_deployer --build ~/.config/fcitx/rime` → exit 0，无输出（增量构建，schema 无变化；custom_phrase 为纯文本 stabledb，运行时直接读取，无需编译） |
| 输入法进程 | fcitx (PID 735161) 运行中；ibus-daemon (PID 210979) 也运行中（ibus 目录不存在，无需处理） |
| 重启方式 | `fcitx-remote -r` 热重载（exit 0，进程保持运行） |

## Pipeline 重新生成（T4.2）

- stdout 关键行：`rime_export: excluded 74 mixed ASCII+CJK term(s)` ✅
- payload：`artifacts/.rime_import_rime_ice.txt`（2114 行，72524 字节）

### Automated Verification 实测

| ID | Command | 实测 |
|----|---------|------|
| V1 | `grep -cP '^[^\t]*[A-Za-z]+[^\t]*[\x{4e00}-\x{9fff}][^\t]*\t' artifacts/.rime_import_rime_ice.txt` | **0** ✅ |
| V2 | `grep -cP '^(?=[^\t]*[A-Za-z])(?=[^\t]*[\x{4e00}-\x{9fff}])[^\t]*\t' artifacts/.rime_import_rime_ice.txt` | **0** ✅ |

## Manual typing verification（人工打字验证 — 待用户填写）

> 请在重启输入法后，在真实输入框中依次键入下列拼音，勾选结果。

- [x] 打 `dao` → 候选首位为「到」，且**不再出现 ITER到DEMO**
- [x] 打 `mo` → **不再出现 L模 / I模 / H模**
- [x] 打 `qiu` → **不再出现 DXTRAN球**

## 附：T4.1 清理命令（复现用）

```bash
MIXED_RE='^(?=[^\t]*[A-Za-z])(?=[^\t]*[\x{4e00}-\x{9fff}])[^\t]*\t'
F=~/.config/fcitx/rime/custom_phrase.txt
grep -cP "$MIXED_RE" "$F"   # 预期 0
```

回滚：`cp -a ~/.config/fcitx/rime/custom_phrase.txt.bak-20260817 ~/.config/fcitx/rime/custom_phrase.txt` → `rime_deployer --build ~/.config/fcitx/rime` → `fcitx-remote -r`

确认人：Xiaokang Zhang｜确认方式：人工打字验证（dao/mo/qiu 候选均正常，无混排词弹出）｜确认日期：2026-08-18
